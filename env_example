SECRET_KEY=your_secret_key_here
DEBUG=False

# -------------------------------------------------------------------------
# Database configuration

# Local / default configuration
DB_ENGINE=django.db.backends.postgresql
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432

# DATABASE_URL=postgresql://postgres:Skytrack976531@localhost:5432/mapwala_db

# --------------------------------------------------------------------------
# For Live Database

# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=your_db_name
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_HOST=your_db_host
# DB_PORT=5432

# DATABASE_URL=postgresql://your_db_user:your_db_password@your_db_host:5432/your_db_name

# --------------------------------------------------------------------------
# For Docker

# Docker Postgres initialization
DOCKER_DB_NAME=your_db_name
DOCKER_DB_USER=your_db_user
DOCKER_DB_PASSWORD=your_db_password

# Django DB connection (must match above password)
DATABASE_URL=postgresql://your_db_user:your_db_password@db:5432/your_db_name

# ----------------------------
# Hosts configuration
# ----------------------------
ALLOWED_HOSTS=api.example.com,admin.example.com,localhost,127.0.0.1

# ----------------------------
# CORS configuration
# ----------------------------

# For development
CORS_ALLOWED_ORIGINS=http://localhost:3000

# For production
# (uncomment and replace with real domains)
# CORS_ALLOWED_ORIGINS=https://example.com,https://admin.example.com

# Gunicorn
GUNICORN_WORKERS=3
GUNICORN_TIMEOUT=120

# App user (entrypoint.sh uses these)
APP_USERNAME=9876543210
APP_PASSWORD=Test@1234

# SuprerUser Configurations
SUPERUSER_USERNAME=admin
SUPERUSER_PASSWORD=mapwalA@1234
SUPERUSER_EMAIL=admin@mapwala.com